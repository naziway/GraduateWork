<UserControl x:Class="UserControls.OrdersUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:local="clr-namespace:UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <ListBox Name="OrderList" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  ItemsSource="{Binding Orders}">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Expander 
                    Header="{Binding Path=.}"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    IsExpanded="False">
                        <Expander.HeaderTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Код замовлення:"  />

                                    <TextBlock Grid.Column="1" Text="{Binding OrderKod}"/>

                                    <TextBlock Grid.Column="2" Text="Дата оформлення:"  />

                                    <TextBlock Grid.Column="3" Text="{Binding StartData, StringFormat=d}"  />

                                    <Button Grid.Column="4" Command="{Binding Command}" Content="Змінити" CommandParameter="{Binding Path=.}"/>
                                    <Grid.InputBindings>
                                        <MouseBinding Gesture="LeftDoubleClick"
                                                      Command="{Binding Command}"
                                                      CommandParameter="{Binding ElementName=OrderList, Path=SelectedItem}"/>
                                    </Grid.InputBindings>
                                </Grid>
                            </DataTemplate>
                        </Expander.HeaderTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="1" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="тип замовлення:"/>
                                <TextBlock Grid.Column="1" Text="{Binding OrderType}"/>
                            </Grid>
                            <Grid Grid.Row="2" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Статус замовлення:"/>
                                <TextBlock Grid.Column="1" Text="{Binding OrderStatus}"/>
                            </Grid>
                            <Grid Grid.Row="3" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="тип замовлення:"/>
                                <TextBlock Grid.Column="1" Text="{Binding StartData,StringFormat=d}"/>
                            </Grid>

                            <Grid.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                                      Command="{Binding Command}"
                                                      CommandParameter="{Binding ElementName=OrderList, Path=SelectedItem}"/>
                            </Grid.InputBindings>
                        </Grid>
                    </Expander>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
