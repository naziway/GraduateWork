<UserControl x:Class="UserControls.ClientListUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <DataGrid HorizontalAlignment="Stretch" ItemsSource="{Binding Clients,IsAsync=True}" AutoGenerateColumns="False"  VerticalAlignment="Stretch">
            <DataGrid.Columns>
                <DataGridTextColumn Width="*" Header="Прізвище" Binding="{Binding FirstName}"/>
                <DataGridTextColumn Width="*" Header="Імя" Binding="{Binding LastName}"/>
                <DataGridTextColumn Width="*" Header="Паспортні данні" Binding="{Binding PasportData}"/>
                <DataGridTextColumn Width="*" Header="Телефон" Binding="{Binding PhoneNumber}"/>
                <DataGridTextColumn Width="*" Header="Дата" Binding="{Binding RegistrationDate,StringFormat=d}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
