<UserControl x:Class="UserControls.ItemsControl.ReviewListBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControls.ItemsControl"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Код замовлення:"  />

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Chartreuse" Grid.Column="1" Text="{Binding Kod}"/>

        <TextBlock  Grid.Column="2" Text="Дата оформлення:"  />

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="3" Text="{Binding OrderDate, StringFormat=d}"  />


        <Button Grid.Column="4" Command="{Binding ViewCommand}" Content="Переглянути" CommandParameter="{Binding Path=.}"/>

        <Grid.InputBindings>
            <MouseBinding Gesture="LeftDoubleClick"
                                                      Command="{Binding ViewCommand}"
                                                      CommandParameter="{Binding ElementName=OrderList, Path=SelectedItem}"/>
        </Grid.InputBindings>
    </Grid>
</UserControl>
