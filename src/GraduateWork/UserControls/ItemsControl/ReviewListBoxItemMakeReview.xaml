<UserControl x:Class="UserControls.ItemsControl.ReviewListBoxItemMakeReview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserControls.ItemsControl"
             mc:Ignorable="d" 
             d:DesignHeight="30" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="30" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="60" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Код замовлення:"  />

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" Grid.Column="1" Text="{Binding Kod}"/>

        <TextBlock  Grid.Column="2" Text="Дата оформлення:"  />

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="3" Text="{Binding OrderDate, StringFormat=d}"  />


        <Button Grid.Column="4" Width="Auto" Command="{Binding Command}" Content="Провести обстеження" CommandParameter="{Binding Path=.}"/>

        <Grid.InputBindings>
            <MouseBinding Gesture="LeftDoubleClick"
                                                      Command="{Binding Command}"
                                                      CommandParameter="{Binding ElementName=OrderList, Path=SelectedItem}"/>
        </Grid.InputBindings>
    </Grid>
</UserControl>
